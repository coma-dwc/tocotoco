<h1>admin/users#indexページ</h1>
<h2>adminのユーザー一覧画面(adminのトップページに設定)</h2>
<div class="container">
  <div class="row">
    <h4 class="col-lg-auto bg-light text-center">ユーザー一覧</h4>
  </div>

  <div class="row justify-content-end">
    <%= link_to admin_users_path do %>
    <i class="fas fa-plus-circle fa-2x" style="color: #bfc5cf;"></i>
    <% end %>
  </div>

  <div class="row justify-content-lg-center">
    <table class="table table-borderless table-hover table-inverse border-top border-bottom">
      <thead class="thead-light">
        <tr>
          <th>ユーザーID</th>
          <th>ニックネーム</th>
          <th>メールアドレス</th>
          <th>ステータス</th>
        </tr>
        </thead>
        <tbody>
        <% @users.each do |user| %>
        <tr>
          <td><%= user.id %></td>
          <td><%= link_to admin_user_path(user.id) do %>
          <%= user.name %>
          <% end %>
          </td>
          <td><%= user.email %></td>
          <td>
            <% if user.is_deleted == true%>
            <p class="text-danger">退会済</p>
            <% else %>
            <p class="text-success">有効</p>
            <% end %>
            </td>
        </tr>
        </tbody>
        <% end %>
    </table>
  </div>
  <div class="row justify-content-lg-center">
    <%= paginate @users %>
  </div>
</div>