<div class="container">
  <div class="row">
    <div class="col-5">
      <h4 class="bg-light">都道府県データ一覧・追加</h4>
      <%= form_with model: @place, url: admin_places_path, method: :post do |f| %>
      <div class="form-check form-check-inline">
        <p>都道府県名&emsp;&emsp;<%= f.text_field :prefectures %>&emsp;&emsp;<%= f.submit '新規登録', class:"btn btn-success" %></p>
      </div>
      <% end %>

      <table class="table">
        <tr class="bg-light">
          <td>都道府県</td>
          <td></td>
        </tr>
        <% @places.each do |place| %>
        <tr>
          <td><%= place.prefectures %></td>
          <td><%= link_to "編集する", admin_place_path(place), class:"btn btn-success" %></td>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>